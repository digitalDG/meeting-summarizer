import type { NextConfig } from "next";

const config: NextConfig = {
  // The WebSocket server runs via server.ts (custom server).
  // Next.js just handles the React app.
};

export default config;
